workflows:
  ios-build:
    name: iOS Build
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      groups:
        - assinatura_ios
      node: latest
      xcode: latest
    scripts:
      - name: Instalar dependências
        script: npm install --legacy-peer-deps
      - name: Build Web
        script: npm run build
      - name: Sincronizar iOS nativo
        script: npx cap sync ios
      - name: Configurar chaves de assinatura (Keychain)
        script: |
          # 1. Decodifica os arquivos
          echo $CERTIFICATE_P12 | base64 --decode > certificate.p12
          echo $PROVISIONING_PROFILE | base64 --decode > profile.mobileprovision
          
          # 2. Cria o cofre e instala o certificado
          keychain_name="build.keychain"
          keychain_password="password"
          security create-keychain -p "$keychain_password" $keychain_name
          security default-keychain -s $keychain_name
          security unlock-keychain -p "$keychain_password" $keychain_name
          security import certificate.p12 -k $keychain_name -P "$CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-keychain-partition-list -S apple-tool:,apple: -s -k "$keychain_password" $keychain_name
          
          # 3. Instala o Provisioning Profile
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
      - name: Aplicar perfis de assinatura no Xcode
        script: |
          cd ios/App
          xcode-project use-profiles
      - name: Resolver Dependências do SPM (Swift)
        script: |
          cd ios/App
          xcodebuild -resolvePackageDependencies -project "App.xcodeproj" -scheme "App"
      - name: Debug de Compilacao Swift (Verifica se o codigo esta quebrado)
        script: |
          cd ios/App
          echo "Iniciando compilação de diagnóstico (ignorando chaves e assinaturas)..."
          xcodebuild clean build -project "App.xcodeproj" -scheme "App" -destination "generic/platform=iOS" CODE_SIGNING_ALLOWED=NO | tee diagnostic.log
    artifacts:
      - "**/build/ios/ipa/*.ipa"
